# UBF Field Constants - Руководство

## Проблема

UBF field ID в Enduro/X - это не простые числа, а **закодированные значения**, содержащие информацию о типе поля. Использование незакодированных номеров (1002, 1012 и т.д.) приводит к некорректной работе UBF API.

### Пример неправильного кода:
```rust
const T_NAME_FLD: i32 = 1002;  // ❌ НЕПРАВИЛЬНО - просто номер
```

### Правильный код:
```rust
const T_NAME_FLD: i32 = 167773162;  // ✅ ПРАВИЛЬНО - закодированный ID с типом STRING
```

## Решение: Автогенерация констант

### 1. Build Script (build.rs)

`endurox-sys/build.rs` автоматически:
- Сканирует директорию `ubftab/` для всех файлов `*.fd.h`
- Читает каждый найденный `.fd.h` файл (сгенерированный `mkfldhdr`)
- Парсит все `#define` с закодированными field ID
- Генерирует единый Rust модуль `ubf_fields.rs` в `OUT_DIR`

### 2. Использование в коде

```rust
use endurox_sys::ubf_fields::*;

// Константы уже содержат правильное кодирование типа
let mut buf = UbfBuffer::new(1024)?;
buf.add_string(T_NAME_FLD, "test")?;      // T_NAME_FLD = 167773162
buf.add_long(T_ID_FLD, 12345)?;           // T_ID_FLD = 33555444
buf.add_double(T_PRICE_FLD, 99.99)?;      // T_PRICE_FLD = 134218749
```

### 3. Обновление существующего кода

Замените все ручные определения констант на импорт:

**Было:**
```rust
const T_NAME_FLD: i32 = 1002;
const T_ID_FLD: i32 = 1012;
const T_PRICE_FLD: i32 = 1021;
```

**Стало:**
```rust
use endurox_sys::ubf_fields::{T_NAME_FLD, T_ID_FLD, T_PRICE_FLD};
```

## Кодирование Field ID

В UBF field ID состоит из:
- **Номер поля** (младшие биты)
- **Тип поля** (старшие биты)

Пример из `test.fd.h`:
```c
#define T_NAME_FLD    ((BFLDID32)167773162)  /* number: 1002  type: string */
#define T_ID_FLD      ((BFLDID32)33555444)   /* number: 1012  type: long */
#define T_PRICE_FLD   ((BFLDID32)134218749)  /* number: 1021  type: double */
```

## Типы полей и их коды

| Тип      | Код | Пример field ID      |
|----------|-----|----------------------|
| SHORT    | 0   | 1030                 |
| LONG     | 1   | 33555444 (1012)      |
| CHAR     | 2   | 67109904 (1040)      |
| FLOAT    | 3   | -                    |
| DOUBLE   | 4   | 134218749 (1021)     |
| STRING   | 5   | 167773162 (1002)     |
| CARRAY   | 6   | -                    |

## Процесс сборки

1. **Генерация C заголовка:**
   ```bash
   cd ubftab
   mkfldhdr test.fd  # Создает test.fd.h
   ```

2. **Build script запускается автоматически:**
   - При `cargo build` читает `test.fd.h`
   - Генерирует `$OUT_DIR/ubf_fields.rs`

3. **Константы доступны через:**
   ```rust
   use endurox_sys::ubf_fields::*;
   ```

## Добавление новых полей

### В существующий файл:

1. Отредактируйте `ubftab/test.fd`:
   ```
   T_NEW_FIELD    100  string  -  "New field"
   ```

2. Перегенерируйте заголовок:
   ```bash
   cd ubftab && mkfldhdr test.fd
   ```

3. Пересоберите проект:
   ```bash
   cargo clean && cargo build
   ```

4. Новая константа `T_NEW_FIELD` будет автоматически доступна

### Новый файл field definitions:

1. Создайте новый файл `ubftab/custom.fd`:
   ```
   $#ifndef _CUSTOM_FD
   $#define _CUSTOM_FD
   
   *base 2000
   
   CUSTOM_FIELD_1    1  string  -  "Custom field"
   CUSTOM_FIELD_2    2  long    -  "Custom ID"
   
   $#endif
   ```

2. Сгенерируйте заголовок:
   ```bash
   cd ubftab && mkfldhdr custom.fd
   ```

3. Пересоберите:
   ```bash
   cargo clean && cargo build
   ```

4. Все поля из `custom.fd.h` будут автоматически добавлены в `ubf_fields` модуль

## Важные замечания

⚠️ **Никогда не используйте простые номера** (1002, 1012 и т.д.) напрямую  
✅ **Всегда используйте** константы из `ubf_fields` модуля  
🔄 **Пересоберите после изменения** `test.fd` или `test.fd.h`  
📝 **Не редактируйте** сгенерированный файл `ubf_fields.rs` вручную  

## Troubleshooting

### Ошибка: "test.fd.h not found"
```bash
cd ubftab && mkfldhdr test.fd
```

### Константы не обновляются
```bash
cargo clean
cd ubftab && mkfldhdr test.fd
cargo build
```

### Неправильные значения полей
Проверьте, что используете импортированные константы, а не старые определения:
```rust
// ❌ Старый код - удалить
const T_NAME_FLD: i32 = 1002;

// ✅ Новый код
use endurox_sys::ubf_fields::T_NAME_FLD;
```
